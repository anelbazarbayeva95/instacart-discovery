services:
  - type: web
    name: instacart-discovery-api
    env: python
    buildCommand: "pip install -r requirements.txt && python data/build_index.py"
    startCommand: "uvicorn api.main:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
