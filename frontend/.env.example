# Copy to .env.local and fill in your values

# Backend API URL (leave blank to use Vite proxy in dev)
VITE_API_URL=

# Anthropic API key — used only if you move the chat endpoint server-side
# ANTHROPIC_API_KEY=your_key_here
